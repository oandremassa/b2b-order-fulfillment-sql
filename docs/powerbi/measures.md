# Measures

Measures are kept in the `Metrics` table and grouped by purpose.

The table contains a hidden calculated column so Power BI treats it as a valid measure table. The visible model objects are the DAX measures used by the report.
